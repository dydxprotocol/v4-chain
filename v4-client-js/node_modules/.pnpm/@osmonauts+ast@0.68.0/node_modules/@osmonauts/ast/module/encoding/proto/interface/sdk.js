import { createProtoEnum } from '../enums';
import { createProtoType } from './interface';
export const createSDKType = (context, name, proto) => {
  return createProtoType(context, name, proto, {
    useOriginalCase: true,
    typeNameSuffix: 'SDKType'
  });
};
export const createEnumSDKType = (context, name, proto) => {
  return createProtoEnum(context, name + "SDKType", proto);
};