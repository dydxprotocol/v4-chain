// This file is @generated by prost-build.
/// LiquidateSubaccountsRequest is a request message that contains a list of
/// subaccount ids that potentially need to be liquidated. The list of subaccount
/// ids should not contain duplicates. The application should re-verify these
/// subaccount ids against current state before liquidating their positions.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct LiquidateSubaccountsRequest {
    /// The block height at which the liquidation daemon is processing.
    #[prost(uint32, tag = "1")]
    pub block_height: u32,
    /// The list of liquidatable subaccount ids.
    #[prost(message, repeated, tag = "2")]
    pub liquidatable_subaccount_ids: ::prost::alloc::vec::Vec<
        super::super::subaccounts::SubaccountId,
    >,
    /// The list of subaccount ids with negative total net collateral.
    #[prost(message, repeated, tag = "3")]
    pub negative_tnc_subaccount_ids: ::prost::alloc::vec::Vec<
        super::super::subaccounts::SubaccountId,
    >,
    /// A map of perpetual id to subaccount open position info.
    #[prost(message, repeated, tag = "4")]
    pub subaccount_open_position_info: ::prost::alloc::vec::Vec<
        super::super::clob::SubaccountOpenPositionInfo,
    >,
}
impl ::prost::Name for LiquidateSubaccountsRequest {
    const NAME: &'static str = "LiquidateSubaccountsRequest";
    const PACKAGE: &'static str = "dydxprotocol.daemons.liquidation";
    fn full_name() -> ::prost::alloc::string::String {
        "dydxprotocol.daemons.liquidation.LiquidateSubaccountsRequest".into()
    }
    fn type_url() -> ::prost::alloc::string::String {
        "/dydxprotocol.daemons.liquidation.LiquidateSubaccountsRequest".into()
    }
}
/// LiquidateSubaccountsResponse is a response message for
/// LiquidateSubaccountsRequest.
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct LiquidateSubaccountsResponse {}
impl ::prost::Name for LiquidateSubaccountsResponse {
    const NAME: &'static str = "LiquidateSubaccountsResponse";
    const PACKAGE: &'static str = "dydxprotocol.daemons.liquidation";
    fn full_name() -> ::prost::alloc::string::String {
        "dydxprotocol.daemons.liquidation.LiquidateSubaccountsResponse".into()
    }
    fn type_url() -> ::prost::alloc::string::String {
        "/dydxprotocol.daemons.liquidation.LiquidateSubaccountsResponse".into()
    }
}
/// Generated client implementations.
pub mod liquidation_service_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    use tonic::codegen::http::Uri;
    /// LiquidationService defines the gRPC service used by liquidation daemon.
    #[derive(Debug, Clone)]
    pub struct LiquidationServiceClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    #[cfg(feature = "grpc-transport")]
    impl LiquidationServiceClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> LiquidationServiceClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::Body>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> LiquidationServiceClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::Body>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::Body>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<
                http::Request<tonic::body::Body>,
            >>::Error: Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            LiquidationServiceClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// Sends a list of subaccount ids that are potentially liquidatable.
        pub async fn liquidate_subaccounts(
            &mut self,
            request: impl tonic::IntoRequest<super::LiquidateSubaccountsRequest>,
        ) -> std::result::Result<
            tonic::Response<super::LiquidateSubaccountsResponse>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic::codec::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/dydxprotocol.daemons.liquidation.LiquidationService/LiquidateSubaccounts",
            );
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(
                    GrpcMethod::new(
                        "dydxprotocol.daemons.liquidation.LiquidationService",
                        "LiquidateSubaccounts",
                    ),
                );
            self.inner.unary(req, path, codec).await
        }
    }
}
