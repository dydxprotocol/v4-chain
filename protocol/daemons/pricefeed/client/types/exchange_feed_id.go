package types

// TODO(DEC-663): replace all values with their real value from the prices module.
// ExchangeFeedId is the unique id for an `ExchangeFeed` in the `Prices` module.
// The id must match the `Prices` module's `ExchangeFeed.Id`.
type ExchangeFeedId = uint32
