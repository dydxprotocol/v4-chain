package types

// TODO(DEC-663): replace all values with their real value from the prices module.
// MarketId is the unique id for a `Market` in the `Prices` module.
// The id must match the `Prices` module's `Market.Id`.
type MarketId = uint32
