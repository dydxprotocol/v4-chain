# This yamllint configuration extends the default configuration as detailed in the link below:.
# https://yamllint.readthedocs.io/en/stable/configuration.html#extending-the-default-configuration.
extends: default

rules:
  line-length:
    max: 120
  document-start:
    present: false
