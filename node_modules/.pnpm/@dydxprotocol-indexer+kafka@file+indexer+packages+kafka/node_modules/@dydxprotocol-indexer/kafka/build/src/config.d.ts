/**
 * Environment variables required for Kafka.
 */
export declare const kafkaConfigSchema: {
    SERVICE_NAME: (varName: string) => string;
    KAFKA_BROKER_URLS: (varName: string) => string;
    KAFKA_CONNECTION_TIMEOUT_MS: (varName: string) => number;
    KAFKA_SESSION_TIMEOUT_MS: (varName: string) => number;
    KAFKA_REBALANCE_TIMEOUT_MS: (varName: string) => number;
    KAFKA_HEARTBEAT_INTERVAL_MS: (varName: string) => number;
    KAFKA_CONCURRENT_PARTITIONS: (varName: string) => number;
    KAFKA_ENABLE_UNIQUE_CONSUMER_GROUP_IDS: (varName: string) => boolean;
    KAFKA_MAX_BATCH_WEBSOCKET_MESSAGE_SIZE_BYTES: (varName: string) => number;
};
declare const _default: {
    SERVICE_NAME: string;
    KAFKA_BROKER_URLS: string;
    KAFKA_CONNECTION_TIMEOUT_MS: number;
    KAFKA_SESSION_TIMEOUT_MS: number;
    KAFKA_REBALANCE_TIMEOUT_MS: number;
    KAFKA_HEARTBEAT_INTERVAL_MS: number;
    KAFKA_CONCURRENT_PARTITIONS: number;
    KAFKA_ENABLE_UNIQUE_CONSUMER_GROUP_IDS: boolean;
    KAFKA_MAX_BATCH_WEBSOCKET_MESSAGE_SIZE_BYTES: number;
} & {
    isDevelopment: () => boolean;
    isStaging: () => boolean;
    isProduction: () => boolean;
    isTest: () => boolean;
};
export default _default;
