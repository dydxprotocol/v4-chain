"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.strip = exports.stringify = exports["default"] = void 0;

var _typeof2 = _interopRequireDefault(require("@babel/runtime/helpers/typeof"));

var strip = function strip(obj) {
  var copy; // Handle the 3 simple types, and null or undefined

  if (null == obj || 'object' != (0, _typeof2["default"])(obj)) return obj; // Handle Date

  if (obj instanceof Date) {
    copy = new Date();
    copy.setTime(obj.getTime());
    return copy;
  } // Handle Array


  if (obj instanceof Array) {
    copy = [];

    for (var i = 0, len = obj.length; i < len; i++) {
      copy[i] = strip(obj[i]);
    }

    return copy;
  } // Handle Object


  if (obj instanceof Object || (0, _typeof2["default"])(obj) === 'object') {
    copy = {};

    for (var attr in obj) {
      if (obj.hasOwnProperty(attr)) {
        switch (attr) {
          case 'loc':
          case 'start':
          case 'end':
            break;

          default:
            copy[attr] = strip(obj[attr]);
        }
      } else {
        copy[attr] = strip(obj[attr]);
      }
    }

    return copy;
  }

  throw new Error("Unable to copy obj! Its type isn't supported.");
};

exports.strip = strip;

var stringify = function stringify(ast) {
  var $2 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
  var $3 = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 2;
  var obj = strip(ast);
  return JSON.stringify(obj, $2, $3);
};

exports.stringify = stringify;
var _default = stringify;
exports["default"] = _default;