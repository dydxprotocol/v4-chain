import { IndexerOrder } from '@dydxprotocol-indexer/v4-protos';
import { OrderFromDatabase, PerpetualMarketFromDatabase, SubaccountFromDatabase } from '../types';
/**
 * Converts an order from the database to an IndexerOrder proto.
 * This is used to resend open stateful orders to Vulcan during Indexer fast sync
 * to uncross the orderbook.
 *
 * @param order
 */
export declare function convertToIndexerOrderWithSubaccount(order: OrderFromDatabase, perpetualMarket: PerpetualMarketFromDatabase, subaccount: SubaccountFromDatabase): IndexerOrder;
/**
 * Converts an order from the database to an IndexerOrder proto.
 * This is used to resend open stateful orders to Vulcan during Indexer fast sync
 * to uncross the orderbook.
 *
 * @param order
 */
export declare function convertToIndexerOrder(order: OrderFromDatabase, perpetualMarket: PerpetualMarketFromDatabase): Promise<IndexerOrder>;
