import { Transaction as ObjectionTx } from 'objection';
import { IsolationLevel } from '../types';
export default class Transaction {
    static transactions: {
        [id: number]: ObjectionTx;
    };
    static nextTxId: number;
    static start(): Promise<number>;
    static setIsolationLevel(id: number, level: IsolationLevel): Promise<void>;
    static commit(id: number): Promise<void>;
    static rollback(id: number): Promise<void>;
    static get(txId: number | undefined): ObjectionTx | undefined;
}
